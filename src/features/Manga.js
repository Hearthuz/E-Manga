import React from 'react'
import Navbar from './Navbar';

function Manga({ className }) {
    return (
        <div className={className}>
            <Navbar />
        </div>
    )
}

export default Manga